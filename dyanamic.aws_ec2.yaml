plugin: amazon.aws.ec2
regions:
  - us-east-1          # or your AWS region
filters:
  tag:Environment: dev # optional filters
keyed_groups:
  - key: tags.Name
    prefix: tag
hostnames:
  - dns-name
compose:
  ansible_host: public_ip_address
